{# This is the template file, do not Edit it! -#}
#pragma once

#include "aimrt_module_cpp_interface/module_base.h"

namespace {{ project_name }}::{{ module_name }} {

class {{ class_name }} : public aimrt::ModuleBase {
 public:
  {{ class_name }}() = default;
  ~{{ class_name }}() override = default;

  aimrt::ModuleInfo Info() const noexcept override {
    return aimrt::ModuleInfo{.name = "{{ class_name }}"};
  }

  bool Initialize(aimrt::CoreRef core) noexcept override;

  bool Start() noexcept override;

  void Shutdown() noexcept override;

 private:
  aimrt::logger::LoggerRef GetLogger() { return core_.GetLogger(); }

 private:
  aimrt::CoreRef core_;
};

}  // namespace {{ project_name }}::{{ module_name }}
{# blank line #}
